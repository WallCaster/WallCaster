{
  "name": "back",
  "version": "1.0.0",
  "description": "wallcaster backend",
  "main": "src/index.ts",
  "repository": "https://github.com/WallCaster/WallCaster",
  "author": "WallCaster",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "nodemon --watch src --exec ts-node src/index.ts --ignore ./src/settings.json",
    "dev": "nodemon --watch src --exec ts-node src/index.ts",
    "build": "tsc -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "socket.io": "^4.5.4"
  }
}
